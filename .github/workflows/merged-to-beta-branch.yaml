name: "Merged To Beta Branch"

on:
  pull_request:
    branches:
      - beta
    types:
      - closed

jobs:
  create-branch-and-pr:
    if: github.event.pull_request.merged == true && github.event.head_ref != 'main'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Delete staging branch
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create --base main --head ${{ github.head_ref }} --title "${{ github.event.pull_request.title }}" --body "${{ github.event.pull_request.body }}" --draft
